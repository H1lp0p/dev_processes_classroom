package com.stuf.grading.domain.model

/**
 * Направление для ручного бонуса/штрафа и коэффициента качества (см. диаграмму критериев).
 */
enum class CriterionDirection {
    /** Добавить баллы при срабатывании условия / включённом тумблере. */
    ADD,

    /** Вычесть баллы. */
    SUBTRACT,
}
